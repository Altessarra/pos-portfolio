# Optional.
# Leave blank if the frontend is served by the backend in production.
# For separate frontend/backend deployments, set this to your backend API URL.
# Example:
# VITE_API_URL=https://your-backend-domain.com/api
